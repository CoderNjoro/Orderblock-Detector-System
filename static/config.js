// Centralized configuration for Enhanced Forex Order Block Analyzer
export const API_ENDPOINT = '/api/proxy'; // Backend proxy endpoint for API requests
export const DEFAULT_SETTINGS = {
    outputSize: 500,
    stopLossPips: 20,
    takeProfitPips: 40,
    // Add more default settings as needed
};
